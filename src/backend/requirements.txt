fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
email-validator>=2.0.0
mcp>=1.0.0
anthropic>=0.40.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg[binary]>=3.2.0

# Cache / sessions
redis>=5.0.0

# Vector memory
mem0ai>=0.1.0
qdrant-client>=1.11.0
google-genai>=1.0.0
groq>=0.18.0

# LLM
langgraph>=0.2.0
requests>=2.32.0

# Auth
bcrypt>=4.0.0
pyjwt>=2.8.0

# Scheduler (cron jobs: morning brief, weekly digest, DNA aggregate, mem compact)
apscheduler>=3.10.0

# Observability (optional — only initialized if SENTRY_DSN is set)
sentry-sdk[fastapi]>=2.0.0

# Tests
pytest>=8.0.0
httpx>=0.27.0
